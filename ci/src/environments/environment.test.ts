export const environment = {
  production: false,
  apiUrl: 'http://hn.domiv.ca/api', //global API URL (for production build)
  baseUrl: window.location.origin + '/gizmo' // Base URL here
  //... more of your variables
};
